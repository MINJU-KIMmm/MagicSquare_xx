# MagicSquare_1004 — RED 작업 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_1004 / MagicSquare_xx |
| 단계 | TDD **RED** — 설계·스켈레톤·브랜치 정리 |
| 일자 | 2026-06-04 |
| 브랜치 | **`red`** (RED), **`green`** (RED 반영 후 GREEN 대기), **`spec`** (RED 제거·`main` 동기) |
| 근거 | `Report/04`, `docs/tdd-red-todo.md`, `.cursorrules`, `.cursor/skills/magic-square-tdd/` |

---

## 1. 세션 목표

Dual-Track RED를 **문서(SSOT)·실패 테스트 스켈레톤·브랜치 규칙**까지 고정한다.

- **RED** → `red` 브랜치 (`tests/`만, `pytest` FAIL)
- **GREEN** → `green` 브랜치 (`src/` 최소 구현)
- **spec** → 문서·스펙만 (RED 커밋 **미포함**)

---

## 2. Git · 브랜치 정리

| 브랜치 | tip (요약) | 내용 |
|--------|------------|------|
| **`red`** | `d0c7202` | RED 설계 + D-LOC-01 skeleton + README 브랜치 매핑 |
| **`green`** | `d0c7202` | `red` fast-forward 병합 — GREEN 시작점 |
| **`spec`** | `300ac7d` | `main`과 동일 — RED 커밋 **제거** (force-push) |
| **`main`** | `300ac7d` | PR 워크플로 문서 |

**커밋 (`main..red`):**

| 해시 | 메시지 |
|------|--------|
| `0a8ece9` | Add RED phase planning docs and README checklist. |
| `c936018` | test(red): D-LOC-01 skeleton and G1 fixture |
| `d0c7202` | docs: clarify TDD branch mapping (red=RED, green=GREEN) |

**PR Compare (RED):** `https://github.com/MINJU-KIMmm/MagicSquare_xx/compare/main...red?expand=1`

---

## 3. 산출물 — 문서·체크리스트

| 파일 | 설명 |
|------|------|
| `docs/tdd-red-todo.md` | Dual-Track RED To-Do SSOT (D-* / U-*) |
| `README.md` | RED 체크리스트, U-IN-01~02 Track A, TDD 브랜치 표 |
| `Report/04. … RED Phase Planning 보고서.md` | RED **설계** 세션 (계획) |
| `Report/05. … RED 작업 보고서.md` | 본 문서 — RED **실행** 세션 |

---

## 4. 산출물 — RED 스켈레톤 (D-LOC-01)

**Phase:** `red | Layer: entity | Track: Logic`  
**Test ID:** `D-LOC-01` (묶음 `D-LOC-02`~`03`은 To-Do에만, 미작성)

| 항목 | 내용 |
|------|------|
| **파일** | `tests/entity/test_d_loc_01.py` |
| **Fixture** | `tests/conftest.py` — `grid_g1` (G1, `0` 빈칸 2개 at **(2,2)·(3,3)** 1-index) |
| **SSOT** | `src/entity/constants.py` — `GRID_SIZE`, `TARGET_SUM`, `MAX_CELL_VALUE`, `BLANK_CELL` |
| **Then (설계)** | `find_blank_coords` → `[(2,2),(3,3)]` row-major |
| **RED 구현** | `pytest.fail("RED: D-LOC-01 — 구현 없음, 의도적 실패")` — assert 본문 없음 |

**pytest (확인):**

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
# → FAILED — Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패
```

**기술 메모:** `tests/entity/` 패키지명이 `entity` import와 충돌 → `conftest`에서 `importlib`로 `src/entity/constants.py` 로드.

---

## 5. Dual-Track RED 범위 (진행 상태)

### Logic · entity

| ID | 상태 |
|----|------|
| D-LOC-01 | ✅ 스켈레톤 + 의도적 FAIL |
| D-LOC-02, D-LOC-03 | ⬜ 예정 |
| D-006, D-007 | ⬜ 예정 |

### Logic · control (Session 3 우선)

| ID | 상태 |
|----|------|
| D-001 ~ D-003 | ⬜ 예정 (Mom Test 3건) |
| D-004, D-005, D-008 | ⬜ 예정 |

### Boundary · UI

| ID | 상태 |
|----|------|
| U-IN-01, U-IN-02 | ⬜ README 설계만 (`test_u_input_validation.py` 미작성) |
| U-IN-03 ~ U-DISP | ⬜ 예정 |

---

## 6. RED 규칙 준수

| 항목 | 준수 |
|------|------|
| `tests/`만 추가 (스켈레톤) | ✅ (`constants.py`는 fixture SSOT 4줄) |
| `skip` / `xfail` / 통과 더미 | ✅ 없음 |
| Logic Domain Mock | ✅ 없음 |
| entity E001~E005 | ✅ emit 없음 |
| 브랜치 | ✅ RED → `red` (이후 `spec`에서 RED 제거) |

---

## 7. 다음 단계

| # | 브랜치 | 작업 |
|---|--------|------|
| 1 | `red` | D-LOC-02~03, D-001~003 RED 스켈레톤 추가 |
| 2 | `red` | U-IN-01~02 boundary RED 스켈레톤 |
| 3 | `green` | `find_blank_coords` 등 **최소 구현** → D-LOC-01 PASS |
| 4 | `spec` | FR-LOC-01·E001~E007 PRD 반영 (문서만) |

---

## 8. 참조

| 문서 | 경로 |
|------|------|
| RED To-Do | `docs/tdd-red-todo.md` |
| RED 설계 보고서 | `Report/04. MagicSquare_1004 RED Phase Planning 보고서.md` |
| Transcript | `Prompting/04. cursor_magic_square_red_work.md` |
| TDD RED Command | `.cursor/commands/tdd-red.md` |

---

## 9. 결론

RED 단계는 **`red` 브랜치**에 설계 문서·**D-LOC-01 의도적 FAIL** 스켈레톤까지 반영되었고, **`green`**은 동일 tip에서 GREEN을 시작할 수 있다. **`spec`**은 RED 없이 `main`과 맞춰 문서·스펙 전용으로 유지한다.
