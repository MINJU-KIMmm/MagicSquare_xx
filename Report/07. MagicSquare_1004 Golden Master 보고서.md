# MagicSquare_1004 — Golden Master 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_1004 / MagicSquare_xx |
| 단계 | **Golden Master** — GREEN PASS 후 승인 기준 고정 |
| 일자 | 2026-06-04 |
| 브랜치 | **`green`** (`e0c5969` — D-SOL-01 + golden) |
| 근거 | `Report/06`, `/golden-master`, `.cursorrules` (`int[6]` 1-index) |

---

## 1. 세션 목표

**D-SOL-01** entity 테스트가 **pytest PASS**인 상태에서, 출력 포맷을 **Golden Master** 파일로 고정한다. 수동 편집으로 통과 우회는 금지하며, 의도적 변경 시 `UPDATE_GOLDEN=1`로만 갱신한다.

| 선행 조건 | 상태 |
|-----------|------|
| D-LOC-01 GREEN | ✅ `find_blank_coords` |
| D-SOL-01 GREEN | ✅ `solve_step_a` |
| `.venv` pytest | ✅ 재현 가능 |

---

## 2. 대상 Test ID

| ID | Layer | 테스트 | Golden 파일 |
|----|-------|--------|-------------|
| **D-SOL-01** | entity | `test_d_sol_01_step_a_success` | `tests/golden/d_sol_01_g1_step_a.approved.txt` |

**Fixture:** G1 — 빈칸 `0` at **(2,2)**, **(3,3)** (1-index, row-major).

---

## 3. Golden Master 인프라

### 3.1 `tests/_approval.py`

| 함수 | 동작 |
|------|------|
| `assert_matches_golden(actual, relative)` | `tests/golden/{relative}` 와 **문자열 완전 일치** 비교 |
| `UPDATE_GOLDEN=1` | golden 파일 **생성·덮어쓰기** (LF `\n` 고정) |
| golden 없음 | `AssertionError` + 생성 명령 안내 |

### 3.2 고정 포맷 (수동 편집 금지)

```text
status: success
track: entity
test_id: D-SOL-01
solution_int6: 2,2,14,3,3,3
error_code: 
```

| 필드 | 규칙 |
|------|------|
| `solution_int6` | `[r1,c1,n1,r2,c2,n2]` — **1-index**, 콤마 구분 |
| `error_code` | 성공 시 빈 값 (boundary E00x는 본 golden 범위 외) |

**의미 (G1, step A):** 행 합=34로 빈칸 채움 — (2,2)→14, (3,3)→3.

### 3.3 구현 연결

| 파일 | 역할 |
|------|------|
| `src/entity/solver_step.py` | `solve_step_a`, `format_step_a_golden` |
| `tests/entity/test_d_sol_01.py` | Act → format → `assert_matches_golden` |

---

## 4. 절차 · 검증 결과

### 4.1 기준 파일 생성

```powershell
$env:UPDATE_GOLDEN="1"
.\.venv\Scripts\python.exe -m pytest tests/entity/test_d_sol_01.py::test_d_sol_01_step_a_success -v
Remove-Item Env:UPDATE_GOLDEN
```

**결과:** PASS — `tests/golden/d_sol_01_g1_step_a.approved.txt` 생성.

### 4.2 matched 검증

```powershell
.\.venv\Scripts\python.exe -m pytest tests/entity/test_d_sol_01.py::test_d_sol_01_step_a_success -v
.\.venv\Scripts\python.exe -m pytest tests/entity/ -v
```

| 실행 | 결과 |
|------|------|
| D-SOL-01 단건 | **matched** · 1 passed |
| entity 전체 | 2 passed (D-LOC-01 + D-SOL-01) |
| diff | **없음** (expected == actual) |

---

## 5. Git · PR

| 항목 | 내용 |
|------|------|
| 커밋 | `e0c5969` — feat(green): D-SOL-01 PASS and golden master approval |
| 푸시 | `origin/green` ✅ |
| PR | **미자동 생성** — Compare URL에서 수동 생성 |

**PR Compare:** https://github.com/MINJU-KIMmm/MagicSquare_xx/compare/main...green?expand=1

---

## 6. 규칙 준수

| 항목 | 준수 |
|------|------|
| golden 수동 편집으로 통과 우회 | ✅ 금지 (포맷은 `format_step_a_golden`만) |
| int[6] 1-index | ✅ |
| entity E001~E005 emit | ✅ |
| UPDATE_GOLDEN 없이 검증 | ✅ matched |

---

## 7. 다음 단계

| # | 작업 | 브랜치 |
|---|------|--------|
| 1 | GitHub PR `main` ← `green` 생성 | — |
| 2 | D-LOC-02~03 RED → GREEN (golden 확장 여부 판단) | `red` / `green` |
| 3 | boundary U-OUT-01 golden (별도 묶음) | `green` 또는 boundary |
| 4 | `refactoring` — Green 고정 후 구조 개선 | `refactoring` |

---

## 8. 산출물 목록

| 파일 | 설명 |
|------|------|
| `tests/_approval.py` | Golden 헬퍼 |
| `tests/golden/d_sol_01_g1_step_a.approved.txt` | 승인 기준 |
| `tests/entity/test_d_sol_01.py` | D-SOL-01 테스트 |
| `src/entity/solver_step.py` | step A 구현 |
| `Report/07. … Golden Master 보고서.md` | 본 문서 |
| `Prompting/06. … Export Transcript.md` | 세션 기록 |

---

## 9. 참조

| 문서 | 경로 |
|------|------|
| GREEN 작업 | `Report/06. MagicSquare_1004 GREEN 작업 보고서.md` |
| README § Golden | `README.md` |
| RED To-Do | `docs/tdd-red-todo.md` |

---

## 10. 결론

**D-SOL-01** Golden Master 파일 생성·**matched** 검증까지 완료되었다. entity 레이어 **GREEN PASS**는 D-LOC-01·D-SOL-01 **2건 PASS**로 확인되었으며, 출력 회귀는 golden 파일로 잠글 수 있다.
